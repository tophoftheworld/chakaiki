rules_version = '2';
// Temporary rules for one-off Firestore import only. Deploy, run migrate, then redeploy firestore.rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
